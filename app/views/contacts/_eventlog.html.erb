<div class="Contact EventForm">
	<span><%= link_to @contact.watches.exists?(user: current_user) ? image_tag("unwatch.png") : image_tag("watch.png"), watch_contact_path(@contact) %></span>
	<%= form_for [@contact, @contact.contact_events.build] do |f| %>
	<table>
		<tr>
			<td colspan="3">
				<%= f.text_area :event_text, placeholder: "Contact Notes" %>
			</td>
		</tr>
		<tr>
			<td colspan="2"></td>
			<td>
				<%= f.submit "Save" %>
			</td>
		</tr>
	</table>
	<% end %>
</div>
<div class="Contact EventSummary">
	<% headers = ['Time', 'Note', 'User'] %>
	<div class="List">
		<span class="Count"><%= @contact.contact_events.count %></span>
		<span class="Tablename">Contact Events</span>
		<div class="ListContents">
			<table class="ListTable">
				<thead>
					<tr>
						<% headers.each do |h| %>
						<th class="ListHeader"><%= link_to h, sort: h %></th>
						<% end %>
					</tr>
				</thead>
				<tbody>
					<%= render @contact.contact_events %>
				</tbody>
			</table>
		</div>
	</div>
</div>